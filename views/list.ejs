<!DOCTYPE html>
<html>

<head>
  <title>Song List</title>
  <style>
    body {
      background-color: #fff;
      /* 배경색은 흰색(#fff)로 설정 */
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
    }

    .white-wrapper {
      /* border: 4px solid #ff0; */
      border: 0px;
      border-radius: 15px;
      padding: 13px 10px 10px 10px;
      width: 420px;
      height: 130px;
      overflow: hidden;
      /* background-color: rgb(245, 235, 235, 0.7); */
      background: no-repeat center/100% url('/img/playlist_background_x-max.png');
      /* 테두리 안의 배경색을 차콜색(#444)으로 설정 */
      display: flex;
      /* 추가: flex 컨테이너로 설정 */
      justify-content: center;
      /* 수정: 가로 방향 오른쪽 정렬 */
      align-items: center;
      /* 추가: 세로 방향 가운데 정렬 */
      flex-direction: column;
    }

    .neon-sign-container {
      display: flex;
      flex-direction: column;
      align-items: center;
      margin-bottom: 5px;
    }

    .neon-sign {
      /* border: 4px solid #ff0; */
      border: 0px;
      border-radius: 15px;
      padding: 10px;
      width: 400px;
      height: 80px;
      overflow: hidden;
      /* background-color: rgb(0, 0, 0, 0.9); */
      background-color: rgb(0, 0, 0, 1);
      /* 테두리 안의 배경색을 차콜색(#444)으로 설정 */
      display: flex;
      /* 추가: flex 컨테이너로 설정 */
      justify-content: flex-end;
      /* 수정: 가로 방향 오른쪽 정렬 */
      align-items: center;
      /* 추가: 세로 방향 가운데 정렬 */
    }

    @font-face {
      font-family: 'GamsengCafe';
      src: url('/css/KNPSKkomi.otf') format('truetype');
    }

    .neon-sign .neon-text {
      white-space: nowrap;
      font-size: 3em;
      /* font-family: 'Shadows Into Light', cursive; */
      font-family: 'GamsengCafe', cursive;
      /* color: transparent; */
      color: #f8efd7;
      -webkit-text-stroke: 1px #f8efd7;
      text-shadow: 0 0 10px #edaae4, 0 0 20px #edaae4, 0 0 30px #edaae4, 0 0 40px #edaae4, 0 0 70px #edaae4;
      animation: neon-animation 10s linear infinite;
    }

    @keyframes neon-animation {
      0% {
        transform: translateX(100%);
      }

      100% {
        transform: translateX(-150%);
      }
    }

    .button-box {
      display: flex;
      justify-content: space-between;
      /* margin-top: 20px; */
    }

    .btn {
      width: 30px;
      height: 30px;
      /* border-style: solid; */
      /* border-color: #202020; */
      border-style: hidden;
      margin-left: 30px;
      margin-right: 30px;
    }

    .btn-text {
      /* height: 30px;
      white-space: nowrap;
      flex-direction: row;
      border-style: hidden;
      margin-left: 30px;
      margin-right: 30px;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      font-family: 'GamsengCafe', cursive;
      font-size: 1.2em;
      font-weight: bold; */

      white-space: nowrap;
      display: flex;
      flex-direction: row; /* 수정: 텍스트를 가로로 나열하도록 변경 */
      justify-content: center;
      align-items: center;
      font-family: 'GamsengCafe', cursive;
      font-size: 1.4em;
      font-weight: bold;
      letter-spacing: 5px;
    }
    .green{
      color: rgb(0, 90, 0);
      padding-left: 0%;
      padding-right: 0%;
      margin-left: 0%;
      margin-right: 0%;
    }
    .red{
      color: red;
      padding-left: 0%;
      padding-right: 0%;
      margin-left: 0%;
      margin-right: 0%;
    }
    .white{
      color: rgb(0, 0, 0, 0);
      padding-left: 0%;
      padding-right: 0%;
      margin-left: 0%;
      margin-right: 0%;
    }
  </style>
  <link href="/css/digital-7.ttf" rel="stylesheet">
</head>

<body>
  <div class="white-wrapper">
    <div class="neon-sign-container">
      <div class="neon-sign">
        <div class="neon-text">
          <%= songs.length> 0 ? songs[0] : '' %>
        </div>
      </div>
    </div>
    <div class="button-box">
      <img src="/img/playlist_right_menu_x-max.png" class="btn" />
      <div class="btn-text">
        <span class="green">Merry Christmas</span>
      </div>
      <img src="/img/playlist_right_menu_x-max.png" class="btn" />
    </div>
  </div>

</body>
<script>
  function autoRefreshPage() {
    location.reload();
  }
  setInterval(autoRefreshPage, 10000);
</script>

</html>